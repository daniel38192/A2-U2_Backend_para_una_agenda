services:
  frontend1_db:
    image: mysql:lts-oraclelinux9
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: frontend1_db
    ports:
      - "3306:3306"
    volumes:
      - frontend1_db_data:/var/lib/mysql
volumes:
  frontend1_db_data:
